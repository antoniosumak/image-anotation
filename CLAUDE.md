# image-anotation

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`antoniosumak/image-anotation`), driven by the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string named after its role. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
